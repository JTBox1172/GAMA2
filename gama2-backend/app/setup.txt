To install requirements run pip install -r requirements.txt;
If you add any new packages ensure that you add the package to the requrements.txt file including version as necassary;
You may also need to create a retun_data and uploaded_data under the user_data folder since git automatically deletes empty folders;